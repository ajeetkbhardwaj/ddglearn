# Core Mathematical & Compute Dependencies
numpy>=1.21.0
scipy>=1.9.0

# Visualization (Optional, install with `pip install .[viz]`)
matplotlib>=3.5.0
polyscope>=2.0.0

# Development & Testing (Optional, install with `pip install .[dev]`)
pytest>=7.0.0
pytest-cov
psutil>=5.9.0